# clipso — context

## stack
- bash 5.x, Termux + Debian + macOS
- backends: termux-clipboard-set | wl-copy | xclip | OSC52 | pbcopy-forward
- install.sh: idempotent, ln -sf, chmod +x, wires PATH in .zshrc/.bashrc

## last-known-good
- clipso.sh: 6536379 — spinner starts after first byte (dd bs=1 count=1)
- install.sh: chmod +x present, no change needed
- deployed: ~/.local/bin/clipso → clipso.sh ✓

## fixes
- 839a23e: --paste flag + mesh clipboard cache
- 813bdec: CLIPSO_NO_SPINNER env var
- 44173d1: -q flag
- 6536379: spinner after first byte — avoids blocking interactive prompts (password/fingerprint)

## known issues
- cp in deploy loses +x — always use install.sh, never cp to deploy
- nssh already copies output internally — wrapping nssh with clipso is redundant

## pending
- none

## session update — 2026-05-27 (privacy_check rewrite)
### architecture
- privacy_check(): rewritten — single awk pass, zero per-line forks (was echo|grep×4 per line)
- display_with_privacy(): new fn — prints all lines numbered in order; flagged lines inline in red at correct position (not pre-printed to stderr)
- Globals: PRIVACY_HITS (int), PRIVACY_INFO_FILE (tmpfile: linenum TAB tag TAB masked)
- Clipboard: flagged lines → "# censored line" (preserves line count, not deleted)
- Warn message: printed AFTER full output, not before
- BYTES recomputed after privacy_check if hits>0

### false-positive reduction (expert strategy)
- CRED: requires word-boundary + assignment context (=/:) + value ≥5 chars + non-placeholder value
- PRIV-IP: RFC1918 strict + valid_ip() octet validation
- MAC: strict 6-octet; excludes 00:00:00:00:00:00 and ff:ff:ff:ff:ff:ff
- PUB-IP: valid_ip() + not private + not safe-DNS + not version-like context (prev char not [a-zA-Z0-9.])
- Self-flagging of own source code eliminated (no PAT_CREDS shell var)

### last-known-good
- clipso: awk rewrite deployed; syntax OK; committed+pushed

## session update — 2026-05-27 (privacy_check rewrite)
### architecture
- privacy_check(): rewritten — single awk pass, zero per-line forks (was echo|grep×4 per line)
- display_with_privacy(): new fn — prints all lines numbered in order; flagged lines inline in red at correct position (not pre-printed to stderr)
- Globals: PRIVACY_HITS (int), PRIVACY_INFO_FILE (tmpfile: linenum TAB tag TAB masked)
- Clipboard: flagged lines → "# censored line" (preserves line count, not deleted)
- Warn message: printed AFTER full output, not before
- BYTES recomputed after privacy_check if hits>0

### false-positive reduction (expert strategy)
- CRED: requires word-boundary + assignment context (=/:) + value ≥5 chars + non-placeholder value
- PRIV-IP: RFC1918 strict + valid_ip() octet validation
- MAC: strict 6-octet; excludes 00:00:00:00:00:00 and ff:ff:ff:ff:ff:ff
- PUB-IP: valid_ip() + not private + not safe-DNS + not version-like context (prev char not [a-zA-Z0-9.])
- Self-flagging of own source code eliminated (no PAT_CREDS shell var)

### last-known-good
- clipso: awk rewrite deployed; syntax OK; committed+pushed

## Session 2026-05-27
### Completed ✓
- `_fmt_size`: replaced `bc` (not in Termux) with pure bash integer arithmetic — no forks, no subshells

### Pending
- Permission denied on $TMP at line 226 — resolved, transient race from stale tmp during broken run. Cannot reproduce.

### Last-known-good
- `_fmt_size` pure-bash patch applied and syntax-checked OK
