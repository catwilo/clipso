# clipso — context

## stack
- bash 5.x, Termux + Debian + macOS
- backends: termux-clipboard-set | wl-copy | xclip | OSC52 | pbcopy-forward
- install.sh: idempotent, ln -sf, chmod +x, wires PATH in .zshrc/.bashrc

## last-known-good
- clipso.sh: 6536379 — spinner starts after first byte (dd bs=1 count=1)
- install.sh: chmod +x present, no change needed
- deployed: ~/.local/bin/clipso → clipso.sh ✓

## fixes
- 839a23e: --paste flag + mesh clipboard cache
- 813bdec: CLIPSO_NO_SPINNER env var
- 44173d1: -q flag
- 6536379: spinner after first byte — avoids blocking interactive prompts (password/fingerprint)

## known issues
- cp in deploy loses +x — always use install.sh, never cp to deploy
- nssh already copies output internally — wrapping nssh with clipso is redundant

## pending
- none
